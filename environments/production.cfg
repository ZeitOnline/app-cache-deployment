[environment]
branch = production
connect_method = ssh
update_method = git-bundle
service_user = app-cache
platform = debian

[hosts]
app-cache03.zeit.de = varnish, dummyvarnishtest

[varnish]
subdomain =
